# 검토 요청 — NELoRa 재검증 정정 라운드

2026-09-21 · 대상 커밋 `f7165f8` · 전체 내용은 `README_NELORA.md`

이번 라운드는 새 결과를 낸 것이 아니라 **초판(`68b5ce4`)의 오류 두 개를 잡고 결론 하나를
철회한** 라운드다. 검토는 "새 숫자가 맞는가" 보다 **"철회가 충분한가, 남긴 주장이 이제
방어 가능한가"** 를 봐주시면 된다.

---

## 1. 지적받은 것과 처리 결과

| # | 지적 | 처리 |
|---|---|---|
| ① | §6 의 D1 변형들이 표준 수신기를 재현 못 했을 수 있다 | **맞음. 결론 철회.** 원인은 지적보다 깊었다 (§2) |
| ② | `comp_alias` 조언이 틀렸다 (복소합이 더 나쁨) | **양쪽 다 맞음.** 블록 수에 따라 방향이 뒤집힌다 (§4) |
| ③ | 생성기 순환시프트 규약이 깨져 있다 | **맞았고, §4 의 τ/ε 까지 바꿨다** (§2) |
| 문구 | "하드웨어 몫은 존재하지 않는다" = CI 없는 null 주장 | 수정. CI 가 0 을 배제해서 "구별 안 됨" 도 못 씀 (§5) |
| 문구 | 헤드라인에서 신경망 이득과 병치 | 요약에서 제거. §8-4 에 별도 실험 필요로 명시 |

---

## 2. 근본 원인은 chirp 생성기였다

`nelora_mf_test.ideal_chirps` 가 순시주파수를 `cumsum` 으로 적분한다. 이산화 오차가
샘플율에 의존하므로 **OSF 마다 다른 파형**이 나온다.

| 성질 | cumsum 판 | 해석적 판 (`nelora_chirp.py`) |
|---|---|---|
| `X[c]` vs `roll(X[0], −OSF·c)` 최대오차 | **2.000** | **0.000** |
| OSF=8 판 데시메이션 vs OSF=1 판 \|상관\| | **0.714** | **1.000** (오차 0) |

이것이 세 가지를 동시에 설명한다:

- 제 표준 수신기가 무잡음에서 23% 밖에 못 맞춘 것
- 교수님 구현이 44% 나온 것
- 초판의 "OSF=1 데시메이션 −7.77 dB" — **저는 이걸 "그 변형은 안 된다" 로 읽었다.
  그게 넓은 결론으로 간 오류다.**

두 생성기는 **정확히 (τ = −1.00 샘플, ε = −0.0625 bin)** 어긋나 있고, 그만큼 보정하면
\|상관\| 이 **1.0000** 이 된다. 따라서 초판 §4 의 오프셋에 그 몫이 섞여 있었다.

| | 초판 (cumsum) | 현재 (해석적) |
|---|---|---|
| τ | +2.00 샘플 | **+1.00 샘플** |
| ε | +0.188 bin | **+0.1562 bin** |
| 정렬 전 프로토타입 \|상관\| | 0.83 | **0.932** |
| 정렬 후 | 0.989 | 0.989 |

**결론(상수 오프셋 하나로 설명된다)은 유지, 숫자는 전부 교체.**

여기에 순서 버그가 겹쳤다. 초판 `d1_decimate` 는 **de-chirp 를 먼저 하고** 대역을 잘랐다.
de-chirp 후 톤은 ±BW 에 걸쳐 있으므로 ±BW/2 로 자르면 신호를 자르는 짓이다.

---

## 3. 철회한 문장

> ~~"최선의 D1 변형(−14.95 dB)도 D2 와 3.6 dB 차이가 끝까지 남는다.
> 구현 선택의 손실이 아니라 비동기 alias 결합 자체의 손실이다."~~

순서를 바로잡으면 표준 수신기가 MF 와 붙는다. **이상 신호 온전성 검사** (SF7, 4000 trials,
타이밍/CFO = 0):

| 검출기 | 10% SER |
|---|---|
| MF (OSF=8 전대역) | **−20.00 dB** |
| **표준 RX (LPF→데시→dechirp)** | **−19.75 dB** |
| 표준 RX (LPF 없이 솎기만) | −10.75 dB |
| NELoRa abs-alias | −16.14 dB |

실데이터에서도 네 SF 전부 **표준 RX − D2 = +0.06 ~ +0.16 dB**.

남긴 것은 지적하신 좁은 서술 그대로다:

> **NELoRa 구현은 de-chirp 이전의 데시메이션을 건너뛰고, 그 결과 3.5~3.9 dB 를 잃는다.**

| SF | D1 → 표준 RX | 95% CI (패킷 부트스트랩) | 표준 RX − D2 |
|---|---|---|---|
| 7 | +3.91 dB | [+3.64, +4.75] | +0.13 |
| 8 | +3.45 dB | [+3.14, +3.72] | +0.16 |
| 9 | +3.53 dB | [+3.24, +3.78] | +0.06 |
| 10 | +3.45 dB | [+3.21, +3.70] | +0.07 |

---

## 4. ② 복소합 — 두 결과가 모두 맞았다

| 결합 | 10% SER (이상 신호) |
|---|---|
| 2블록 abs (NELoRa 원본) | −16.14 dB |
| 2블록 복소합 | **−16.77 dB** (abs보다 0.63 dB 좋음) |
| 8블록 abs | −12.29 dB |
| 8블록 복소합 | **−10.75 dB** (abs보다 1.5 dB 나쁨) |

방향이 블록 수에 따라 뒤집힌다. 제가 2블록(신호가 있는 ±BW 영역)을, 교수님이 8블록을
쓰신 차이다. **어느 쪽도 "올바른 버전" 이 아니며**, 그 프레임은 `README_NELORA.md` §9-3
에 근거 없음으로 명시 폐기했다.

> 교차검증: `8블록 복소합 = −10.75` 와 `LPF 없이 솎기만 = −10.75` 가 정확히 일치한다.
> M-FFT 전 블록의 복소합은 시간영역 8배 솎기와 같은 연산이다. 독립 경로 둘이 같은 값을
> 내는 것이 구현 방증.

---

## 5. 문구 수정

**D2→D3**: 패킷 부트스트랩 결과 **+0.11 dB [+0.05, +0.20]**. CI 가 0 을 배제하므로
"구별되지 않는다" 로도 못 쓴다. 채택한 표현:

> "0.11 dB [+0.05, +0.20] — 통계적으로 0 은 아니지만 크기가 무시할 수준"

**헤드라인**: 신경망 이득(1.84~2.35 dB)과의 병치를 요약표에서 제거하고, §8-4 에
"같은 축에서 신경망까지 돌리는 별도 실험이 필요" 로 분리했다.

---

## 6. 살아남은 것 (재검증 완료)

§3 바닥 분석은 올바른 생성기에서 **더 강해졌다**. 평가 123패킷 / 8364심볼:

| | 초판 | 현재 |
|---|---|---|
| 검출기 간 오류 겹침 | 96.2% | **99.2%** |
| 셋 다 틀릴 때 예측 일치 | 97.2% | **98.6%** |
| 오프셋 | −1 bin 편향 | **−1: 465 / 490** (+1 없음) |
| 상위 10% 패킷의 오류 점유 | 57.5% | 55.9% |

§1 라벨 출처 기각, §6 delay-Doppler 축퇴(능선 기울기 = 정확히 1/OSF), 밀린 패킷
(추출 창이 정수 bin 만큼 어긋남, 4/123) 도 유지.

---

## 7. 여기를 쳐주시면 됩니다 (알고 있는 약점)

1. **신경망 미실행** — 가장 큰 구멍. NELoRa DNN 을 같은 정렬 조건에서 돌리지 않았으므로
   논문 수치와의 비교는 아직 못 쓴다. 다음 순위 1번.
2. **부트스트랩이 잡음 실현을 고정한다.** 같은 심볼·같은 잡음에 대한 대응비교라 CI 가
   좁게 나온다. 잡음까지 재추출하면 넓어진다. §5 의 +0.11 [+0.05, +0.20] 이 특히 그렇다.
3. **표준 RX − D2 = +0.1 dB 의 정체를 확인 안 했다.** 브릭월 LPF 가 chirp 의 wrap
   불연속 때문에 대역외 에너지 1.1% 를 버린다(생성기 자체검사에서 brick \|상관\| 0.9889).
   그 손실과 크기가 맞는데, **검증은 안 했다.** 맞다면 +0.1 dB 는 물리적으로 설명되는
   잔차이고, 아니면 아직 뭔가 남아 있다는 뜻이다.
4. **+3.5 dB 는 상한에 가까운 추정.** 브릭월은 이상적 필터다. 실제 유한 길이 필터는
   이보다 조금 나쁘므로 NELoRa 가 잃는 실제 몫은 이보다 작을 수 있다.
5. **표본 흔들림이 0.37 dB.** SF7 을 n=4000/boot 1000 으로 돌리면 D1→표준RX 가 +4.28,
   n=2500/boot 400 이면 +3.91. 그래서 헤드라인을 단일값이 아니라 **3.5~3.9 dB** 범위로 썼다.
6. **τ 가 SF9/10 에서 +0.75 로 약간 다르다.** τ 격자 해상도가 0.25 샘플이라 그 한계인지
   실제 SF 의존성인지 구분 안 했다.
7. **D3 은 SF7 에서만 성립.** SF8/9/10 코드 커버리지 21/11/9%.
8. **초판 생성기로 낸 수치가 일부 남아 있다.** §3 헤드라인은 재생성했으나, §6 의 축퇴
   수치와 밀린 패킷 표는 cumsum 판 실행 결과 그대로다. 결론은 생성기에 의존하지 않지만
   숫자는 재생성이 남았다.

---

## 8. 재현

```bash
cd verification
python nelora_chirp.py 7                                    # 생성기 자체검사 (오차 0 확인)
python nelora_stdrx.py ideal --sf 7                         # 온전성: 표준 RX vs MF
python nelora_stdrx.py real ../NeLoRa_Dataset/7 --sf 7 --n 4000 --boot 1000
python nelora_stdrx.py sweep ../NeLoRa_Dataset --sfs 7,8,9,10 --n 2500 --boot 400
```

`nelora_mf_test.ideal_chirps` 에는 사용 금지 주석을 달고, 초판 재현을 위해 함수는 남겼다.

---

## 9. 방법론적으로 배운 것

두 오류를 모두 잡아낸 것은 **이상 신호 온전성 검사** 하나였다 — "타이밍/CFO = 0 인 합성
신호에서 표준 수신기가 MF 와 붙는가". 실데이터로 바로 갔으면 둘 다 못 잡았다. 초판은
변형 다섯 개를 서로 비교했을 뿐 **이론이 아는 정답과 대조하지 않았다.**

앞으로 이 저장소에 새 검출기 팔을 추가할 때는 이상 신호 검사를 먼저 통과시키는 것을
규칙으로 삼는다.
