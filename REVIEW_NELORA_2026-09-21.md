# 검토 요청 — NELoRa 재검증 (2라운드)

2026-09-21 · 전체 내용은 `README_NELORA.md` · 1라운드 기록은 이 문서 §5

**상태: 모든 항목 종료.** DNN 재학습까지 마쳐 표가 전부 채워졌습니다 (§7).

---

## 0. 지난 검토 지적 처리 결과

| # | 지적 | 결과 |
|---|---|---|
| 최우선 | 그들 Python 평가 baseline 이 데시메이션하는지 확인하라 | **확인. 안 한다.** 전제 유지. 그리고 더 큰 것이 나왔다 (§1) |
| §7-3 | 브릭월 손실 0.097 dB 로 닫힌다 | **닫았다.** SF 별로 예측/관측 대조 (§4) |
| CI | SF7 만 위팔 3배 — SER 임계 스윕하라 | **했다.** 바닥 효과였고, 그들 조건에서는 사라진다 (§2) |
| 부트스트랩 | 잡음까지 재추출하라 | **완료.** 200복제, 구간 안 넓어짐, 비대칭 0.8× 로 해소 (§6-2) |
| 조건부 문장 | 가정 명시하고 쓰라 | **가정이 대부분 걷혔다.** 축이 거의 같다 (§3) |
| 프레이밍 | τ 를 "오프셋" 아닌 "규약 차이" 로 | 반영 (README §4) |
| 프레이밍 | D1 정렬 둔감성을 본문에 명시 | 반영. **+0.09 dB** 로 재측정 (§4) |
| 잔여 | stale 수치 재생성 | **했다.** 결과가 더 선명해졌다 (§4) |

---

## 1. 최우선 항목 — 확인했고, 예상보다 큽니다

`daibiaoxuwu/NeLoRa_Dataset` (NELoRa-Bench) `main.py` 원문.

### 1-1. 데시메이션 없음 — 전제 유지

```python
def decode_loraphy(data_in, num_classes, downchirp):
    upsampling = 100
    chirp_data = data_in * downchirp                       # OSF=8 그대로
    fft_raw = fft(chirp_data, len(chirp_data) * upsampling)
    cut1 = fft_raw[:num_classes*upsampling]; cut2 = fft_raw[-num_classes*upsampling:]
    return round(np.argmax(abs(cut1) + abs(cut2)) / upsampling) % num_classes
```

우리 D1 은 그들 평가 baseline 이지 허수아비가 아니다.

### 1-2. 그런데 데이터셋이 baseline 기준으로 걸러져 있습니다

```python
if decode_loraphy(chirp_raw, num_classes, downchirp) == truth_idx:
    datax.append(...)
```

무잡음에서 baseline 이 **이미 맞히는 심볼만** 남긴다. baseline 은 무한 SNR 에서
구성상 100% 다.

직접 걸러 보니 **477/8364 = 5.70%** 가 빠지고, 몰린 패킷이 제 §3 바닥 분석이 지목한
것과 **같습니다**:

```
pkt147(57/68), pkt84(54/68), pkt177(39/68), pkt83(26/68), pkt142(26/68)
```

제가 "오류율 50% 넘는 패킷 84·142·147·177" 로 찾은 것과 일치하고, 5.70% 가 제가 잰
바닥 5.9% 와 맞습니다. **그들 필터가 버리는 심볼과 제 바닥 분석이 지목한 심볼이 같아서**,
서로를 독립적으로 확인해 줍니다.

**부정행위가 아니라 데이터 정제입니다.** 잘린 심볼은 ±1 bin 어긋난 것들이고 어느
검출기든 틀립니다(오류 겹침 99.2%). 문제는 "버린다" 가 아니라 **게이트키퍼가 baseline
자신이라 baseline 의 무잡음 오류율이 구성상 0% 가 된다**는 비대칭입니다.

그리고 그 비대칭은 **제게 불리한 쪽**입니다:

| | D1 − 표준RX (10% SER) |
|---|---|
| 필터 적용 (그들 조건) | **+3.48 dB** |
| 필터 없음 | +4.67 dB |

**필터는 baseline 을 1.19 dB 돕습니다.** 그들 조건을 그대로 쓰는 것이 제게 보수적이라,
"유리한 조건을 골랐다" 는 의심은 성립하지 않습니다.

### 1-3. 그들 `test()` 는 분할을 쓰지 않습니다

`train()` 은 `random_split(data, [train_size, test_size])` 로 9:1 을 나누지만,
공개된 `test()` 는 `all_dataloader = DataLoader(data, ...)` 로 **전체 데이터셋**을
평가합니다. 체크포인트는 그중 90% 로 학습된 것입니다.

**저자들도 README 에 명시해 두었습니다** — *"test() is run on the whole dataset; Please
manually split the datasets and specify another folder if testing is needed."*
숨겨진 결함이 아니라 문서화된 한계입니다. 다만 체크포인트가 그 데이터의 90% 로 학습된
것이라면 **DNN 쪽만** 유리해지는 비대칭이 남습니다(baseline 은 학습이 없어 영향 없음).
출처가 불확실하므로 **사실만 기록**하고 함의는 유보했습니다 — 말씀대로 논문 초안에도
넣지 않습니다. DNN 돌릴 때 as-run / held-out 두 번 재서 확정합니다.
(분할도 패킷이 아니라 **심볼 단위**라 같은 패킷이 양쪽에 들어갑니다.)

---

## 2. SER 임계 스윕 — 지적이 맞았고, 원인은 바닥이었습니다

그들 조건(필터 적용), **아무도 정렬을 받지 않는** 비교:

| 격차 | 10% | 20% | 30% | 편차 |
|---|---|---|---|---|
| **as-run D1 − 표준RX** (필터 적용) | **+3.48** | **+3.20** | **+2.93** | **0.55 dB** |
| as-run D1 − 표준RX (필터 없음) | +4.67 | +3.06 | +2.97 | 1.70 dB |

10% 만 보면 필터 유무로 +3.48 ↔ +4.67 흔들리지만, **20/30% 는 양쪽 다 +2.9~+3.2 dB**
로 붙습니다. SF7 CI 의 위팔이 3배였던 것도 같은 바닥 효과였습니다.
**헤드라인은 그들 임계(10% SER)의 단일값 +3.48 dB 로 쓰고, 이 스윕은 강건성 근거로
각주에 둡니다** (§3-1).

전체 표:

| 검출기 | 10% | 20% | 30% |
|---|---|---|---|
| **D1 `decode_loraphy` U=100, 정렬없음 (as-run)** | −15.31 | −16.89 | −17.99 |
| D1 U=1, 정렬없음 | −16.00 | −17.09 | −18.12 |
| **표준 RX, 정렬없음** | **−18.79** | **−20.09** | **−20.93** |
| 표준 RX, 정렬적용 | −19.38 | −20.52 | −21.34 |
| D2 이상적 MF | −19.63 | −20.69 | −21.54 |
| D3 경험적 MF | −19.82 | −20.81 | −21.69 |

무잡음 오류: D1 **0.00%** (구성상), 표준 RX 0.20%.

---

## 3. 조건부 문장 — 가정이 대부분 걷혔습니다

1.84~2.35 dB 는 SenSys 가 아니라 **NELoRa-Bench (ICLR'23) 초록** 수치이고, 그 논문이
쓰는 데이터셋이 우리가 가진 바로 그것입니다.

| 항목 | 그들 | 우리 | 차이 |
|---|---|---|---|
| 데이터셋 | 27,329 심볼 (초록) | `.mat` 파일 수 **27,329** | **완전 일치** |
| baseline | `decode_loraphy` | 동일 재현 | 없음 |
| 임계 | `axhline(y=0.9)` = 10% SER | 10% SER | 없음 |
| SNR 정의 | 평균 진폭 | 평균 전력 | **+0.001 dB** |

SNR 정의 차는 `10log10(mean|y|²/(mean|y|)²)` 이고 LoRa chirp 이 일정 포락선이라
**+0.001 dB** 입니다. 두 정의로 돌린 표가 숫자까지 동일하게 나옵니다.

### 3-1. 지적하신 범위 혼동 — 고쳤습니다

초록의 1.84~2.35 는 **설정(SF/BW) 간** 범위이고 제 +2.93~3.48 은 **SF7 하나의 임계 간**
범위였습니다. 나란히 놓은 것은 잘못이었습니다. 헤드라인을 단일값으로 바꿨습니다:

> **SF7 / BW125k, 10% SER, 그들 baseline·그들 데이터 필터 기준 +3.48 dB.
> 임계를 20/30% 로 바꿔도 +3.20 / +2.93 로 유지된다.**

### 3-2. SF7 전용 수치 — 논문 그림 없이 나왔습니다

그들 저장소에 SF7 평가 행렬이 들어 있습니다
(`matlab/evaluation/{sf7_v1,baseline_error_matrix}_7_125000.mat`).
`evaluation.m` 이 `plot(SNR_list, 1-error_matrix)` 로 그리므로 `error_matrix` 는 정확도입니다.

| 임계 | baseline | NELoRa DNN | 이득 |
|---|---|---|---|
| **10% SER** | −16.03 dB | −17.49 dB | **+1.46 dB** |
| 20% | −17.17 | −18.70 | +1.54 |
| 30% | −17.99 | −19.57 | +1.59 |

**SF7 의 DNN 이득은 +1.46 dB** 로, 초록 범위 1.84~2.35 의 아래가 아니라 **범위 밖
아래**입니다. "1.84 가 SF7 인지 SF10 인지 모른다" 의 답은 **SF7 은 1.84 보다도 낮다**
입니다. 임계를 바꿔도 1.46~1.59 로 안정적입니다.

⚠️ **그래도 제 +3.48 에서 직접 빼면 안 됩니다.** 세 가지가 다릅니다:

1. 그 행렬은 **SenSys 저장소**의 것으로 MATLAB 이 만든 **옛 per-SNR 데이터셋** 기준입니다.
   제가 측정한 Bench 릴리스가 아닙니다.
2. 거기 baseline 은 `generate_baseline.m` 의 `abs_decode = 0`, 즉 **`chirp_comp_alias`**
   (cut1·cut2 상대위상을 100단계 탐색하는 코히런트 결합)입니다. Bench 의
   `decode_loraphy`(abs)보다 **강한** baseline 입니다. 같은 저장소에서 MATLAB 은 comp,
   Python 은 abs 를 씁니다.
3. 제 +3.48 은 Bench 의 abs baseline 대비입니다.

그래서 현재 말할 수 있는 것은 **방향**까지입니다. 확정은 같은 배치에서 DNN 을 돌려야
합니다(§6-1). 참고로 그 MATLAB baseline 도 `upsamping_factor = 100`, OSF=8,
**데시메이션 없음**이라 전제는 SenSys 경로에서도 유지됩니다.

### 3-3. +3.48 을 둘로 쪼갰습니다

| 요인 | 크기 |
|---|---|
| 제로패딩 `upsampling=100` (그들 기본값, vs U=1) | **−0.69 dB** |
| de-chirp 이전 데시메이션 생략 | **−2.79 dB** |
| 합계 | **−3.48 dB** |

그들 기본 설정이 자기 baseline 을 0.69 dB 깎고 있습니다. "3.5 dB 를 잃는다" 보다
**"데시메이션 2.8 + 제로패딩 0.7"** 이 정확하고, 무엇을 고치면 되는지에 답합니다.

---

## 4. 닫은 항목들

### §7-3 브릭월 손실 — 닫힘

| SF | 대역내 전력 | 예측 손실 | 관측 (표준RX−D2) |
|---|---|---|---|
| 7 | 0.982641 | **+0.076 dB** | +0.13 dB |
| 8 | 0.987339 | +0.055 dB | +0.16 dB |
| 9 | 0.990824 | +0.040 dB | +0.06 dB |
| 10 | 0.993385 | +0.029 dB | +0.07 dB |

같은 자릿수이고 **SF 가 오를수록 둘 다 줄어드는 경향이 일치**합니다. 관측이 0.03~0.10 dB
크지만 교차점 추정 잡음 범위입니다. "완전히 설명" 이라고는 안 쓰고 **"대부분 설명"** 으로
뒀습니다.

### D1 정렬 둔감성 — 본문에 명시

| 검출기 | 정렬 전 → 후 (10% SER) |
|---|---|
| D1 abs-alias | −14.33 → −14.24 (**+0.09 dB**) |
| 표준 RX | −18.00 → −18.41 (−0.41) |
| D2 이상적 MF | −18.06 → −18.54 (−0.48) |

정렬은 MF 계열을 돕고 D1 에는 거의 영향이 없습니다. 그래서 **정렬 없는 비교(§2)가 더
보수적**이고, 헤드라인은 그쪽을 씁니다.

### stale 수치 재생성 — 더 선명해졌습니다

축퇴 능선: 65/2145 점, 기울기 **+0.1250 = 정확히 1/OSF** (초판과 동일).

밀린 패킷은 훨씬 깨끗해졌습니다:

| 패킷 | −1 bin | +0 bin | **+1 bin** | +2 bin |
|---|---|---|---|---|
| 84 | 100.0% | 100.0% | **0.0%** | 100.0% |
| 147 | 100.0% | 100.0% | **0.0%** | 100.0% |

초판은 +1 bin 에서 16.2% / 20.6% 였는데, 생성기를 고치니 **정확히 0%** 입니다.
한 빈 이동으로 완전히 설명됩니다.

---

## 5. 이번 라운드에 새로 철회한 것

### "U=2/4 는 폴딩 인덱스 매핑 버그"

| U | 1 | 2 | 4 | 10 | 100 |
|---|---|---|---|---|---|
| 10% SER | −14.30 | −8.52 | −11.78 | −13.20 | −13.19 |

U≥10 에서 수렴하므로 매핑은 안 깨졌습니다. 제로패딩이 탐색 잡음 빈을 U 배로 늘리는
**실재 손실**이고, U=2/4 가 특히 나쁜 건 `round(argmax/U)` 반올림과 잔차 오프셋이 겹친
것으로 보입니다. 중요한 건 **그들 기본값이 U=100** 이라는 점 — 초판의 U=1 D1 은 그들
baseline 의 **관대한** 판이었습니다.

### "축이 달라 직접 비교 불가" (1차 개정판 §8-4)

과했습니다. 실제 축 차이는 SNR 정의도 임계도 아니고 **데이터셋 필터**였고, 재현 가능합니다.

### "데이터셋의 고정 오프셋"

τ 는 심볼 절단 규약 차이일 수 있고 그들 파이프라인이 이를 겪는다는 증거가 없습니다.
**"정렬을 맞춰야 한다"** 로만 쓰고 원인 귀속은 유보했습니다.

(1라운드 철회분 — cumsum 생성기, "비동기 alias 결합 자체의 손실" — 은
`README_NELORA.md` §9-1, §9-2 에 그대로 있습니다.)

---

## 6. 여기를 쳐주시면 됩니다

1. **신경망 — 닫혔습니다. 다만 세 번 걸렸고 두 번은 제 버그였습니다.**

   "DNN이 약한 게 아니라 제 파이프라인이 틀렸을 가능성" 지적이 맞았습니다. 두 개였습니다.

   | 시도 | 무엇이 틀렸나 | DNN 이득 (micro) |
   |---|---|---|
   | 1차 | 그들 `add_noise` 의 `dataX /= mean(abs(dataX))` 누락 | +0.24 |
   | 2차 | 정규화는 고쳤으나 `.eval()` 을 불렀다 | **−0.75** |
   | **3차** | **그들 `test()` 는 `.eval()` 을 부르지 않는다** | **+0.85** |

   정규화는 argmax 검출기에 무해해서 "생략" 이라고 주석까지 달아놨었는데, 신경망은
   `mean|x|=1` 에 학습된 것이라 결정적이었습니다. 지적하신 입력 정규화 가설 그대로입니다.

   두 번째는 더 고약합니다. `.eval()` 이 그들 `train()` 안에만 있고 `test()` 에는 없습니다.
   모델에 `BatchNorm2d` 8개와 `Dropout(0.2)/(0.5)` 가 있으므로 **그들 평가는 train 모드로
   돌아갑니다** — BatchNorm 이 테스트 배치 통계를 쓰고 Dropout 이 켜진 채로.

   ### 하네스 검증 — 재구현물끼리 비교하지 않았습니다

   말씀하신 ③ 대로, 그들 `main.py` 의 `test()` 를 설정 두 줄만 바꿔(데이터 경로, SNR 격자)
   무수정 실행하고 `test_sf7.pkl` 과 대조했습니다.

   | | 그들 `test()` | 제 하네스 | 차이 |
   |---|---|---|---|
   | baseline (micro) | −13.17 | −13.11 | **+0.07** |
   | DNN (micro) | −13.91 | −13.96 | **−0.04** |
   | baseline (macro) | −12.88 | −12.78 | **+0.09** |
   | DNN (macro) | −13.36 | −13.30 | **+0.05** |

   **네 지점 모두 0.1 dB 이내.** 2차까지는 baseline 만 맞고 DNN 이 1.5 dB 어긋났는데,
   그 비대칭이 버그를 DNN 경로로 좁혀줬습니다.

   ### 표가 채워졌습니다

   ```
   SF7 / BW125k / 10% SER / 그들 필터 / 그들 SNR 축 / 그들 모델모드 / 같은 배치
     decode_loraphy (U=100)   : -13.11 dB   기준 0
     NELoRa DNN               : -13.96 dB   +0.85 dB
     표준 RX (학습·정렬 없음)   : -16.57 dB   +3.46 dB
                                            -------------
     표준 RX - DNN            : +2.61 dB   (macro 로는 +3.22 dB)
   ```

   DNN 거동도 이제 디노이저 모양입니다 — −12~−18 에서 +1.1~+3.9%p 로 돕고 고SNR 에서만
   살짝 손해. 말씀하신 "디노이저는 SNR 이 낮을수록 더 기여해야 한다" 가 충족됩니다.

   ### 고SNR 온전성 (지적 ①)

   | SNR | baseline | DNN | 표준RX |
   |---|---|---|---|
   | +10 | 100.0% | 99.3% | 99.9% |
   | +5 | 100.0% | 98.7% | 99.9% |
   | 0 | 100.0% | 99.1% | 99.9% |

   DNN 이 고SNR 에서 baseline 보다 0.7~1.3%p 낮은 것은 train 모드의 Dropout 때문으로
   보입니다. 100% 는 아니지만 파이프라인을 의심할 수준은 아닙니다.

   ### 덤으로 나온 보고할 사실

   **그들 보고 수치는 추론 시 BatchNorm 이 테스트 배치 통계를 씁니다.** 따라서
   `batch_size` 와 배치 구성에 의존합니다. 그리고 Dropout 이 켜져 있는데도 train 모드가
   eval 모드보다 1.5 dB 나은 것은 **running BN 통계가 수렴하지 않았다**는 뜻이고,
   README 의 *"only trained for a limited amount of time"* 과 맞습니다.
   통상적 추론(`--model-mode eval`)으로는 DNN 이 **−0.75 dB** 로 자기 baseline 에도
   못 미칩니다.

   ### 체크포인트 품질 — 재학습 중

   공개 체크포인트의 +0.85 는 그들 발표 SF7 +1.46 에 못 미칩니다. 지금 그들 `train()` 으로
   이어서 학습 중입니다(에포크당 19초, 100에포크 ≈ 32분). 끝나면 같은 하네스로 다시 잽니다.

   **held-out 은 하지 않기로 했습니다.** 지금 as-run 조건이 이미 DNN 에 유리한데
   (모델이 평가 심볼의 ~90% 를 학습에서 봄) 2.6 dB 집니다. 제대로 나누면 DNN 이 더
   나빠지지 좋아지지 않으므로, 우리 주장 방향에서 as-run 이 보수적입니다. 인덱스 매핑
   배관을 안 건드리는 쪽이 낫다고 판단했습니다 — 오늘 낸 버그 셋이 전부 그런 배관이었습니다.

2. ~~§5 표의 부트스트랩이 아직 대응비교.~~ **해결.**

   복제마다 패킷을 재표집하고 **잡음도 새로 뽑아** 200복제 돌렸습니다 (필터 적용,
   D1 as-run − 표준RX 정렬판):

   | 임계 | 중앙값 | 95% CI | 비대칭비 |
   |---|---|---|---|
   | 10% | +4.21 dB | [+3.88, +4.48] | 0.8× |
   | 20% | +3.64 dB | [+3.47, +3.75] | 0.7× |
   | 30% | +3.30 dB | [+3.13, +3.45] | 0.8× |

   **잡음을 재추출해도 구간이 안 넓어집니다.** 그리고 지적하신 SF7 비대칭(3.1×)이
   **0.8× 로 해소**됐습니다 — 바닥 효과였다는 진단이 확인됩니다.

3. ~~as-run 조건의 SF8/9/10 미측정.~~ **해결.** 말씀대로 U=10 으로 돌렸습니다.
   먼저 SF7 에서 U=10 과 U=100 을 직접 대조해 대체를 검증했습니다(D1 −15.29 vs −15.31,
   격차 +3.49 vs +3.48, 필터 통과율·패킷 목록 동일).

   **아무도 정렬을 받지 않는 비교:**

   | SF | 무잡음 오류 (D1/표준RX) | **10%** | 20% | 30% |
   |---|---|---|---|---|
   | 7 | 0.00% / 0.20% | **+3.49** | +3.24 | +3.01 |
   | 8 | 0.00% / 0.00% | **+3.50** | +2.82 | +2.87 |
   | 9 | 0.00% / 0.00% | **+3.77** | +3.09 | +3.15 |
   | 10 | 0.00% / 0.00% | **+3.50** | +2.83 | +2.96 |

   SF8/9/10 은 필터 조건에서 무잡음 오류가 **0.00%** 라 바닥 효과가 아예 없고, 그래서
   필터 유무에도 거의 안 움직입니다(SF9: +3.77 → +3.79). SF7 만 +3.49 → +4.86 으로
   튀는데, §3 의 −1 bin 심볼이 SF7 패킷에 몰려 있기 때문입니다.

   ⚠️ SF9/SF10 이 처음에 "교차 없음" 으로 나왔습니다. `nelora_stdrx.py` 에서 이미 한 번
   고친 "SNR 격자가 SF 를 안 따라간다" 함정을 `nelora_asrun.py` 에 반영 안 한 탓입니다.
   같은 실수를 두 번 했고, 지금은 격자가 `-3*(sf-7)` 만큼 자동으로 내려갑니다.

4. ~~stale 수치 재생성.~~ **해결** (이전 라운드에서 완료, §4).

---

## 7. 최종 — 오늘 확정된 것

```
SF7 / BW125k / 10% SER / 그들 필터 · 그들 SNR 축 · 그들 모델모드 / 같은 배치
  decode_loraphy (U=100)        : -13.11 dB   기준 0
  NELoRa DNN (100에포크 재학습)   : -14.49 dB   +1.39 dB   <- 그들 발표 +1.46 재현
  표준 RX (학습 · 정렬 없음)      : -16.57 dB   +3.46 dB
                                               ----------
  표준 RX - DNN                 : +2.08 dB    (macro 로는 +2.24 dB)
```

그리고 baseline 대비 격차는 **SF7~10 에서 +3.49 ~ +3.77 dB** 로 유지됩니다.

한 줄로:

> **de-chirp 이전의 데시메이션 한 단계가, 학습도 정렬도 없이, 그들 신경망보다
> 2.1 dB 더 번다.**

---

## 8. 남은 것 / 쓸 때 주의

1. **`.eval()` 건은 사실로만 쓰고 단정하지 말 것.** 그들 `test()` 가 `.eval()` 을 안
   부르므로 보고 수치가 배치 통계에 의존하는 것은 코드에서 확인된 사실입니다. 다만
   "그래서 수치가 부풀려졌다" 는 별개 주장이고, 우리는 그들 조건(train 모드)을 그대로
   써서 비교했으므로 그 주장을 할 필요도 없습니다.

2. **학습 프로세스가 두 개 겹쳐 돌았습니다** (`TEST ACC` 104줄, 체크포인트 200개).
   둘 다 같은 릴리스 체크포인트에서 같은 시드로 이어 학습한 것이라 모델은 사실상
   동일하지만, 최종 가중치가 둘 중 어느 프로세스 것인지는 특정 못 합니다. 다시 돌리면
   깔끔해집니다.

3. **held-out 은 안 했습니다.** as-run 이 이미 DNN 에 유리한데(모델이 평가 심볼의 ~90% 를
   학습에서 봄) 2.1 dB 지므로, 우리 주장 방향에서는 as-run 이 보수적입니다.
   `random_split` 이 torch 시드를 안 잡아 복원도 불가능합니다.

4. **SNR 축은 여전히 합성 AWGN** 입니다(그들도 그렇습니다). 절대 dB 가 아니라 격차만.

5. **SF7 의 +1.39 vs 그들 발표 +1.46 일치는 정확한 대응이 아닙니다.** 그들 수치는
   SenSys per-SNR 데이터셋에 `comp_alias` baseline 기준이고 우리는 Bench 에 abs 기준
   입니다(§5-3). 정합성 신호로만 쓰세요.

---

## 9. 방법론적으로 배운 것

1라운드: **이상 신호 온전성 검사**가 cumsum 생성기와 `d1_decimate` 순서를 잡았습니다.
2라운드: **그들 소스를 직접 읽은 것**이 필터·U=100·축 일치를 알려줬습니다.
3라운드: **그들 스크립트를 무수정으로 돌려 대조한 것**이 정규화 누락과 `.eval()` 을
잡았습니다.

세 라운드 내내 틀린 것은 **전부 제가 재구현한 쪽**이었습니다. 규칙 셋으로 남깁니다:

- 새 검출기 팔은 **이상 신호 검사**를 먼저 통과시킨다.
- 남의 수치와 비교하기 전에 **그 수치를 낸 코드를 읽는다.**
- 재구현이 맞는지는 재구현끼리 비교해 알 수 없다. **원본을 돌려서 대조한다.**